# Step 19 leads (verified)

Range searched: `e6c3289804a67ea0bb6a86fadbe454dd93b8d855..llvmorg-19.1.7` (fork merge base to the 19 release).
Release notes: `llvm/docs/ReleaseNotes.rst` (not `.md` at 19) and `clang/docs/ReleaseNotes.rst` at `llvmorg-19.1.7`.
Fork diff used for impact: `e6c3289804a67..6547028f925c` (PULP_18_HEAD).

## Leads from PORTING.md section 4 (row 19)

| Lead | Verdict | Upstream commit | Effect on the fork |
|---|---|---|---|
| Debug info moved from `llvm.dbg.*` intrinsics to debug records | verified (LLVM notes "Changes to the LLVM IR" and "Changes to the Debug Info") | `dc726c340392` Reapply#4 "[RemoveDIs] Load into new debug info format by default in LLVM (#89799)"; `094572701dce` [RemoveDIs] Print IR with debug records by default (#91724) | None expected. The fork's passes are MIR passes (`PULP/*.cpp`, `Snitch/SNITCHFrepLoops.cpp`, `RISCVExpandS{SR,DMA}Insts.cpp`, `PULPExpandPseudoInsts.cpp`), and none of them uses IR debug intrinsics. `CGStmt.cpp` only calls `CreateIntrinsic`. No fork test contains `llvm.dbg`/`!dbg`. |
| `RISCVISAInfo.cpp` moved from `lib/Support` to `lib/TargetParser` | verified | `733a87783cfa` [RISCV] Split code that tablegen needs out of RISCVISAInfo. (#89684) | Cluster registration. The code is now in `llvm/lib/TargetParser/RISCVISAInfo.cpp` and `llvm/include/llvm/TargetParser/RISCVISAInfo.h`, plus new `llvm/lib/Support/RISCVISAUtils.cpp`. The unit test moved to `llvm/unittests/TargetParser/RISCVISAInfoTest.cpp`, so the fork's +26-line test hunk has to follow it. |
| Extension table became TableGen-generated from `RISCVFeatures.td` | verified | `80628ee0d555` [RISCV] Generate RISCVISAInfo table from RISCVFeatures.td. (#89955); also `b64e483785bf` [RISCV][TableGen] Generate RISCVTargetParserDef.inc from the new RISCVExtension tblgen information. (#89335), `451e853e512b` [RISCV] Flatten the ImpliedExts table in RISCVISAInfo.cpp (#89975) | Cluster registration plus `RISCVInstrInfoXsmallfloatGen.td`. The fork's 27 hand-added `SupportedExtensions` rows (xpulpv 2.0, xdma, xfrep, xssr, xmempool, and 22 smallfloat `xf*`, all 0.1) no longer have a table to go into. Each one must become a `RISCVExtension<name, major, minor, desc>` def instead of a plain `SubtargetFeature`: in `RISCVFeatures.td` for FeaturePULPExtV2/Xfrep/Xdma/Xssr/Xmempool, and in the generator of `RISCVInstrInfoXsmallfloatGen.td` for `xf*`. `FeatureHasNoFdiv` ("nofdiv") is not an ISA extension and stays a `SubtargetFeature`. **Silent-behaviour risk:** at 19 the emitter builds a CPU's default march only from `RISCVExtension` records (`RISCVTargetDefEmitter.cpp:97-110`), so the `snitch` and `mempool-rv32` CPUs in `RISCVProcessors.td` would silently lose their X* extensions if they are left as plain features. |
| `BuiltinsRISCV.def` became a `.td` | verified | `a8d4a024e6be` [Clang][RISCV] Refactor builtins to TableGen | Cluster clang-builtins. The fork's 181 `TARGET_BUILTIN` lines in the deleted `.def` must be rewritten as records in `clang/include/clang/Basic/BuiltinsRISCV.td`. Upstream's `class RISCVBuiltin` hardcodes the spelling `"__builtin_riscv_" # NAME`, so the fork's `__builtin_pulp_*` and `__builtin_ssr_*`/dma builtins need their own class that sets `Spellings` explicitly (`TargetBuiltin`/`Spellings` in `BuiltinsBase.td`). |
| RISC-V builtin checks moved from `SemaChecking.cpp` to `SemaRISCV.cpp` | verified | `a640a2e0205f` [clang] Introduce `SemaRISCV` (#92682); preceded by `4f5bc4bb55a8` [clang][NFC] Rename `SemaRISCVVectorLookup.cpp` into `SemaRISCV.cpp` | Cluster clang-builtins. The fork's PULP immediate checks in `Sema::CheckRISCVBuiltinFunctionCall` move to `SemaRISCV::CheckBuiltinFunctionCall` (`clang/lib/Sema/SemaRISCV.cpp`), with calls going through `SemaRef.`. That hunk also has to take the rename in the next table (`SemaBuiltinConstantArg*` became `BuiltinConstantArg*`). `SemaRISCV.cpp` is a new file for the fork, so the fork surface moves rather than grows. |
| More `ConstantExpr` kinds removed | verified (LLVM notes: icmp, fcmp and shl constant expressions removed) | `deab451e7a7f` [IR] Remove support for icmp and fcmp constant expressions (#93038); `76d3ab2cc333` [IR] Remove support for shl constant expressions (#96037) | None. The fork code has no `ConstantExpr` use. |
| `getInt8PtrTy` family removed | false for step 19 | Removed before the merge base: `7b9d73c2f90c` [NFC] Remove Type::getInt8PtrTy (#71029) (2023-11-07) | None. The fork does not use it. |

## Leads from PORTING.md section 4 ("every step" row), checked for 19

| Lead | Verdict | Upstream commit | Effect on the fork |
|---|---|---|---|
| NPM migration in codegen (pass registration, analyses) | verified | `79d0de2ac37b` [CodeGen][NewPM] Port `machine-loops` to new pass manager (#97793); `837dc542b151` [CodeGen][NewPM] Split `MachineDominatorTree` into a concrete analysis result (#94571) | Cluster passes. `MachineLoopInfo` and `MachineDominatorTree` are no longer legacy passes. `PULPHardwareLoops.cpp`, `PULPFixupHwLoops.cpp` and `SNITCHFrepLoops.cpp` have to change as follows: `addRequired<MachineLoopInfo>` becomes `...WrapperPass`, `getAnalysis<MachineLoopInfo>()` becomes `getAnalysis<MachineLoopInfoWrapperPass>().getLI()`, the MDT calls get the same change with `.getDomTree()`, and so do the `INITIALIZE_PASS_DEPENDENCY` lines. `7652a5940701` Reland "[NewPM][CodeGen] Port selection dag isel to new pass manager" (#94149) also touches `RISCVISelDAGToDAG.{h,cpp}`/`RISCV.h`, but only as context next to the fork hunks. |
| `RISCVPassConfig` hook changes | verified | `28233408a2c8` [CodeGen] [ARM] Make RISC-V Init Undef Pass Target Independent ... (#77770); `1a58e88690c1` [RISCV] Move RISCVInsertVSETVLI to after phi elimination (#91440); `0ebe48f068c0` [RISCV] Move RISCVInsertVSETVLI after CSR/VXRM passes (#91701); `52187b9f2e7e` [RISCV] Move RISCVDeadRegisterDefinitions to post vector regalloc (#90636); `c74ba57e0bd9` [RISCV] Convert AVLs with vlenb to VLMAX where possible (#97800) (renames `RISCVFoldMasks.cpp` to `RISCVVectorPeephole.cpp`) | Cluster passes, in `RISCVTargetMachine.cpp` and `CMakeLists.txt`. Expect textual conflicts: `initializeRISCVInitUndefPass` was removed next to the fork's `initialize*` block, `addPreRegAlloc` was rewritten around the fork's 5 `addPass` lines, and the `RISCVFoldMasks.cpp` context line in CMake is gone. Order change: at 18 the fork passes ran after `RISCVInsertVSETVLI`. At 19 that pass is inserted after PHIElimination/RegisterCoalescer, so the fork passes now run before it. This matters only for RVV code and shows up in pipeline tests. |
| RISC-V base class renames in TableGen | false for 19 (no class or def that the fork references was removed). Replaced by the name-collision lead below. | Scan: every identifier on fork `+` lines in `*.td` checked against class/def/defm names present at the base and gone at 19. No hits. Renames in range such as `016ce9ed5cd3` [RISCV] Rename FeatureRVE to FeatureStdExtE. NFC (#89174) are not used by the fork. | None from renames. |
| `llc` output drift in pipeline and FileCheck tests | verified | 14 commits in range touch `llvm/test/CodeGen/RISCV/O3-pipeline.ll`, e.g. `1a58e88690c1`, `a833fa7d3ef0` [RISCV] Move Machine Copy Propagation Pass before Branch relaxation pass (#97261), `28233408a2c8` | Cluster tests. `O0/O3-pipeline.ll` need regeneration (test-regen role). |

## Leads the table missed (found in notes or range, affecting fork files)

| Lead | Verdict | Upstream commit | Effect on the fork |
|---|---|---|---|
| TableGen def name collisions with new upstream CORE-V/WCH defs | verified | `56f0ecd6db92` [RISCV] Implement Intrinsics Support for XCValu Extension in CV32E40P (#85603) adds `powerOf2Minus1` and `trailing1sPlus1` (`RISCVInstrInfoXCV.td:719-720`); `3c5f929ad093` [RISCV] Add QingKe "XW" compressed opcode extension (#97925) adds `uimm6_lsb0` (`RISCVInstrInfoXwch.td:62`) | Cluster insn-tablegen. `RISCVInstrInfoXpulp.td:31,66,78` define the same three names, so llvm-tblgen stops with duplicate-definition errors. Note that the fork's `powerOf2Minus1` is a `TImmLeaf` while upstream's is an `ImmLeaf`, so they are not interchangeable. Rename the fork's defs or reuse upstream's only after checking the semantics. |
| `Sema::SemaBuiltinConstantArg*` renamed | verified | `298f8f73e1d8` [clang][NFC] Remove "Sema" prefix from Sema-related functions (#87914) | Cluster clang-builtins. There are 13 fork calls to `SemaBuiltinConstantArg`/`SemaBuiltinConstantArgRange`. They become `SemaRef.BuiltinConstantArg`/`BuiltinConstantArgRange` (`Sema.h:2372,2376`). |
| `StringRef::equals` removed | verified | `de483ad51389` [ADT] Deprecate StringRef::equals (#92351); `3fa409f2318e` [ADT] Remove StringRef::equals (#98735) | Cluster clang-driver. The fork's `-mno-fdiv` handling in `clang/lib/Driver/ToolChains/Arch/RISCV.cpp` calls `feat.equals("+fdiv")`, which no longer compiles. Use `==`. |
| `llvm::makeArrayRef` removed | verified | `c70d3874e95b` [ADT] Remove makeArrayRef and makeMutableArrayRef (#79719) | Cluster clang-frep-pragma. `clang/lib/Parse/ParsePragma.cpp` (`Info.Toks = llvm::makeArrayRef(ValueList).copy(...)`) has to use `ArrayRef(ValueList)`. |
| RVV calling-convention rewrite (`allocateRVVReg`/`FirstMaskArgument` removed) | verified | `91dd844aa499` Recommit [RISCV] RISCV vector calling convention (2/2) (#79096) (#87736) | Cluster codegen-core. The fork hunk in `RISCV::CC_RISCV` (`RISCVISelLowering.cpp`) that passes PULP `v2i16`/`v4i8` in GPRs when `hasPULPExtV2()` wraps `allocateRVVReg(ValVT, ValNo, FirstMaskArgument, ...)`. At 19 that call is `RVVDispatcher.getNextPhysReg()` (line 19167), and `CC_RISCV` takes an `RVVArgDispatcher &`. The fork branch must keep going to `ArgGPRs` before the dispatcher is consulted. This is semantic: it is the v4i8/v2i16 ABI. |
| XCVmem post-increment codegen added in the same functions as Xpulp post-increment | verified | `2afea7296812` [RISCV] Codegen support for XCVmem extension (#76916) | Cluster codegen-core. Upstream added a `hasVendorXCVmem()` branch at the top of `getPostIndexedAddressParts` (`RISCVISelLowering.cpp:21251`), a `setIndexedLoad/StoreAction` block (line 1443), and XCVmem indexed-load selection in `RISCVISelDAGToDAG.cpp:1531`. These are the same places as the fork's `getIndexedAddressPartsPulp`, `tryPulpIndexedLoad` and `setIndexed*Action(POST_INC, ...)` hunks, so expect conflicts. Use upstream's XCVmem structure as the reference. |
| RISC-V disassembler `getInstruction` split | verified | `5569c219d35c` [RISCV] Split RISCVDisassembler::getInstruction into a 16-bit and 32-bit version. (#90254); `ac45bb5cbc4e` [RISCV] Consistently use uint32_t in Disassembler decode functions. NFC | Cluster mc. The fork's `TRY_TO_DECODE_FEATURE(RISCV::FeaturePULPExtV2, DecoderTableRV32Xpulp32, ...)` moves to `getInstruction32` (next to XCVbi, `RISCVDisassembler.cpp:621`). The fork decoders `DecodePulpV{2,4}RegisterClass` and `decodeUImmMinus1Operand` use `uint64_t` for the value, while upstream uses `uint32_t`. That compiles, but should match upstream. |
| MC: `MCAsmLayout` parameter removals | verified (no fork-hunk signature change) | e.g. `4289c422a8b4` [MC] Remove the MCAsmLayout parameter from MCObjectWriter::recordRelocation, `8f7887ad5347` [MC] Remove the MCAsmLayout parameter from MCAsmBackend::relax* | Cluster mc. `RISCVAsmBackend.cpp` changes only in context. The fork only adds `case` labels and `Infos` rows. `ELFRelocs/RISCV.def` and `RISCVFixupKinds.h` are untouched upstream in range. |

## Reference patterns at 19 (tag `llvmorg-19.1.7`)

- Extension registration: `llvm/lib/Target/RISCV/RISCVFeatures.td:25` (`class RISCVExtension<name, major, minor, desc, implies, fieldname = !subst("Feature","Has",NAME)>`). The CORE-V examples are at `RISCVFeatures.td:1207-1262` (`FeatureVendorXCVelw`, `XCVbitmanip`, `XCVmac`, `XCVmem`, `XCValu`, `XCVsimd`, `XCVbi`), each followed by its `HasVendorXCV*` `Predicate`+`AssemblerPredicate`. The default fieldname rule maps `FeaturePULPExtV2` to `HasPULPExtV2`, so the existing subtarget getters keep working.
- Generated ISA table consumed at `llvm/lib/TargetParser/RISCVISAInfo.cpp:52-56` (`GET_SUPPORTED_EXTENSIONS`) and `:811` (`GET_IMPLIED_EXTENSIONS`). The emitter is `llvm/utils/TableGen/RISCVTargetDefEmitter.cpp:47` (extensions) and `:97` (`printMArch`, which only takes `RISCVExtension` records).
- ISA help unit test: `llvm/unittests/TargetParser/RISCVISAInfoTest.cpp:858` (`RiscvExtensionsHelp`, with the xcv rows around line 998).
- Instruction definitions: `llvm/lib/Target/RISCV/RISCVInstrInfoXCV.td` (DecoderNamespace per extension, e.g. `:164` XCValu, `:528` XCVmem; patterns under `let Predicates = [HasVendorXCValu, IsRV32]` at `:201`). It is included from `RISCVInstrInfo.td:2082`, and the fork's `.td` includes go next to it.
- Intrinsics: `llvm/include/llvm/IR/IntrinsicsRISCV.td:1904-1906` includes the vendor intrinsic files (`IntrinsicsRISCVXCV.td`), which is where the Xpulp/Xsnitch includes go.
- Clang builtins: `clang/include/clang/Basic/BuiltinsRISCV.td:15` (`class RISCVBuiltin`, which forces the `__builtin_riscv_` prefix) and `clang/include/clang/Basic/BuiltinsBase.td:80,94` (`Spellings`, `TargetBuiltin.Features`). CORE-V has no clang builtins at 19.
- Clang Sema: `clang/lib/Sema/SemaRISCV.cpp:559` (`SemaRISCV::CheckBuiltinFunctionCall`; immediate checks use `SemaRef.BuiltinConstantArgRange`, e.g. `:630`), and the declaration is in `clang/include/clang/Sema/SemaRISCV.h`.
- Post-increment ISel: `RISCVISelLowering.cpp:1443` (XCVmem indexed actions) and `:21246-21270` (`getPostIndexedAddressParts` XCVmem branch), and `RISCVISelDAGToDAG.cpp:1531` (XCVmem indexed load selection).
- Disassembler vendor tables: `llvm/lib/Target/RISCV/Disassembler/RISCVDisassembler.cpp:534` (`getInstruction32`), with the XCVbi entry at `:621`.
- Legacy wrapper usage for hardware loops: `llvm/lib/Target/Hexagon/HexagonHardwareLoops.cpp:121-122,371-372,387-389` (what `PULPHardwareLoops.cpp` was derived from).
- Pass pipeline: `llvm/lib/Target/RISCV/RISCVTargetMachine.cpp:110-131` (initializers) and `:549-563` (`addPreRegAlloc`, with InsertVSETVLI now inserted via `insertPass`).
