# Step 19 conflict: registration

Pick: `4979514c5823` "[pulp] registration: extensions, features, processors, CSRs"
Worktree: `wt/int-19` (branch `port/19`), prev base `e6c3289804a6` (fork's LLVM 18 merge base), new base `llvmorg-19.1.7`.
Auto-merged cleanly: `llvm/lib/Target/RISCV/RISCVSubtarget.h` (`hasExtXsmallfloat()`), and the `mhartid` -> `def MHARTID` rename in `RISCVSystemOperands.td`.

## Paths

| Path | Conflict type | Upstream change | Resolution |
|---|---|---|---|
| `llvm/lib/Support/RISCVISAInfo.cpp` | deleted by us (upstream moved it) | `733a87783cfa` "[RISCV] Split code that tablegen needs out of RISCVISAInfo. (#89684)" renamed it to `llvm/lib/TargetParser/RISCVISAInfo.cpp`; `80628ee0d555` "[RISCV] Generate RISCVISAInfo table from RISCVFeatures.td. (#89955)" replaced the hand-written `SupportedExtensions[]` table with one generated by TableGen from every `RISCVExtension` record in `RISCVFeatures.td`. | Relocation with format conversion: the fork's table entries are now expressed as `RISCVExtension<name, major, minor, desc>` records in `RISCVFeatures.td` (see next row); nothing is added to `llvm/lib/TargetParser/RISCVISAInfo.cpp`. Old path `git rm`-ed. |
| `llvm/lib/Target/RISCV/RISCVFeatures.td` | both modified (append at EOF; upstream added `FeatureForcedSWShadowStack`) | same as above (`80628ee0d555` introduced `class RISCVExtension`) | Kept upstream's block, re-added the fork's block after it. `xpulpv` (2.0), `xfrep`, `xdma`, `xssr`, `xmempool` (0.1) converted from `SubtargetFeature` to `RISCVExtension`, following upstream's CORE-V declarations (`FeatureVendorXCVbitmanip`, `RISCVFeatures.td` line 1215 at `llvm/lib/Target/RISCV/RISCVFeatures.td` in the int-19 tree). The default field name (`!subst("Feature","Has",NAME)`) gives exactly the fork's old field names (`HasPULPExtV2`, `HasExtXfrep`, ...), so no `fieldname` argument is needed and `RISCVSubtarget` accessors are unchanged. Versions are the ones from the fork's old ISAInfo table. `nofdiv` was never in the ISAInfo table, so it stays a plain `SubtargetFeature`. All `Has*` predicates kept verbatim. |
| `llvm/lib/Target/RISCV/RISCV.td` | both modified (sched model includes) | upstream added `RISCVSchedSiFiveP600.td`, `RISCVSchedSyntacoreSCR3.td`, `RISCVSchedXiangShanNanHu.td` includes | Kept all upstream includes, then the fork's `RISCVSchedMempool.td` and `RISCVSchedSnitch.td`. |
| `llvm/lib/Target/RISCV/RISCVProcessors.td` | both modified (append at EOF) | upstream added tune features to `XIANGSHAN_NANHU` and the new `SPACEMIT_X60` processor | Kept upstream's side unchanged, appended the fork's `MEMPOOL` and `SNITCH` processors verbatim (3-arg `RISCVProcessorModel`; the new tune-feature parameter defaults to `[]`). Dropped the fork's trailing blank line at EOF (`diff --check`). |
| `llvm/lib/Target/RISCV/RISCVSystemOperands.td` | both modified (append at EOF) | upstream added the Smrnmi CSRs (`mnscratch`, `mnepc`, `mncause`, `mnstatus`) | Kept upstream's CSRs, appended the fork's `TRACE` custom CSR (0x7D0, requires `FeatureExtXmempool`) verbatim. |
| `llvm/unittests/Support/RISCVISAInfoTest.cpp` | deleted by us (upstream moved it) | `733a87783cfa` renamed it to `llvm/unittests/TargetParser/RISCVISAInfoTest.cpp` | Test relocation, not a rewrite: the fork's 26 added lines in the `PrintSupportedExtensions` expected string were inserted verbatim (same text, same padding) at the same alphabetical positions in the new file (`xdma`..`xpulpv` after `xcvsimd`; `xssr` after `xsifivecflushdlone`, upstream's new last `xs*` entry). Old path `git rm`-ed. |

## Known follow-ups (not fixed here, by design)

- The unit test expectation is known not to match at 19 yet: upstream widened the name column by one character (to fit `xsifivecdiscarddlone`), and the relocated lines keep the LLVM 18 padding. This is whitespace only; it is for a `test-regen` task, which is the only role allowed to change it.
- ESCALATE: the 21 smallfloat extensions (`xfalthalf` ... `xfexpauxvecaltquarter`) were in the fork's LLVM 18 ISAInfo table, but their features are declared in `llvm/lib/Target/RISCV/RISCVInstrInfoXsmallfloatGen.td` (insn-tablegen cluster, next pick), as plain `SubtargetFeature`s. At 19 the ISAInfo table is generated only from `RISCVExtension` records, so unless those 21 declarations become `RISCVExtension<"xf...", 0, 1, ...>`, `-march=..._xfalthalf...` will be rejected and the unit test expectation above cannot pass. They cannot be declared in this commit (they would be duplicate definitions once the next pick adds the generated file). `RISCVInstrInfoXsmallfloatGen.td` says it is "Automatically generated by parse_opcodes (noah95/riscv-opcodes)", an external generator not in the fork, so the decision is: regenerate with a patched generator, or hand-edit the generated file's 21 feature defs (`SubtargetFeature<"xf..", "HasExtXf..", "true", desc>` -> `RISCVExtension<"xf..", 0, 1, desc>`). To be decided at the insn-tablegen pick or the build-fix phase.
- `SNITCH` in `RISCVProcessors.td` references the smallfloat features, which only exist after the insn-tablegen pick; this was already the case in the LLVM 18 stack, so the tree is not expected to TableGen until that pick lands.

Markers check: `git diff --check` and `git diff --cached --check` clean; no `<<<<<<<` in the resolved paths.
