# 19/T005: regen report

Authorization: owner-approved formatting-only exception narrowed by conductor to 25 spaces after actual verification in `notes/19/owner-test-exceptions.md`, independently proposed/reviewed in `notes/19/test-policy-proposal-review.md`. No LLVM updater supports the C++ raw string; this scoped exception permits applying the corrected 25-space patch.

Command: `git -C wt/19-T005 apply notes/19/T005.corrected.patch` (patch path made absolute for invocation).

| Test file | Function / block | Class | Before (instructions) | After (instructions) | Explanation |
|---|---|---|---|---|---|
| llvm/unittests/TargetParser/RISCVISAInfoTest.cpp | RiscvExtensionsHelp.CheckExtensions expected-output raw string, 25 fork extension rows; longest 21-character name unchanged | cosmetic | N/A | N/A | Exactly one ASCII padding space inserted before each version; all names, versions, order, input and assertions preserved. |

No CHECK blocks or instruction expectations changed. Instruction-mix check: lp., p.l*/p.s*, pv., p.mac, frep., scfg* all unchanged (no instruction sequences in this block). E004 dependency is a separate production registration commit, not a regeneration change.
