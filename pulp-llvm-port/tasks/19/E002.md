# Task 19/E002

Written by the conductor. One task is one root cause.

- step: 19
- task_id: E002
- role: worker
- step_base_tag: llvmorg-19.1.7
- prev_tag: e6c3289804a67ea0bb6a86fadbe454dd93b8d855
- integration_head: 7c27289bc3a1a39d0727f00d21dec5aacab0784a
- worktree: /home/ubuntu/llvm-project/pulp-llvm-port/wt/19-E002
- build_dir: /home/ubuntu/llvm-project/pulp-llvm-port/build/19-E002
- branch: work/19/E002
- owner_cluster: clang-builtins; fixup subject: `fixup! [pulp] clang-builtins: RISC-V builtins and their Sema checks`
- build_targets: clang-tablegen-targets (the ClangBuiltins/BuiltinsRISCV .inc generation), then obj.clangBasic
- verified leads: /home/ubuntu/llvm-project/pulp-llvm-port/steps/19/leads.md
- change note path: /home/ubuntu/llvm-project/pulp-llvm-port/notes/19/E002.md
- result path: /home/ubuntu/llvm-project/pulp-llvm-port/tasks/19/E002.result.json
- previous attempts: none

## The problem

Build error cluster E002 from steps/19/errors-r1.json (log logs/build-int-19-r1.log), kind compile, 1 occurrence(s).

Signature: `Unknown Type: _Vector<N, short>`

Sites: clang/include/clang/Basic/BuiltinsRISCVXpulp.td:21

First sample:
```
/home/ubuntu/llvm-project/pulp-llvm-port/wt/int-19/clang/include/clang/Basic/BuiltinsRISCVXpulp.td:21:7: error: Unknown Type: _Vector<2, short>
  let Prototype = prototype;
      ^
```

Background (conductor decision, see PROGRESS.md escalations and notes/19/conflict-clang-builtins.md): the 19.1.7 builtins emitter `clang/utils/TableGen/ClangBuiltinsEmitter.cpp` cannot parse `_Vector<N, T>`; support was added upstream in `508263824f4e` "[Clang] Start moving X86Builtins.def to X86Builtins.td (#106005)" (LLVM 20). The conductor's plan is option (a): backport only that commit's `_Vector` parser change into ClangBuiltinsEmitter.cpp, unchanged in behaviour, with an `Upstream-File-Edit: backport of 508263824f4e _Vector support; disappears at step 20` trailer, rather than changing the fork builtins to `_ExtVector` (that would change their types). ClangBuiltinsEmitter.cpp is not owned by any cluster; commit it with the clang-builtins fixup subject. Verify after the fix that the generated prototype strings for the vector builtins equal the fork's LLVM 18 `.def` strings (e.g. `V2s`, `V4c`), modulo the documented dropped `U` before `V` on parameters.


## Procedure

Follow /home/ubuntu/llvm-project/pulp-llvm-port/agent-sops/worker.sop.md with the parameters above.
Path mapping: documents that say /local/home/albsposi/pulp-llvm-port mean /home/ubuntu/llvm-project/pulp-llvm-port. Builds run natively (no container).
