# Task 19/E005

Written by the conductor. One task is one root cause.

- step: 19
- task_id: E005
- role: worker
- step_base_tag: llvmorg-19.1.7
- prev_tag: e6c3289804a67ea0bb6a86fadbe454dd93b8d855
- integration_head: 7c27289bc3a1a39d0727f00d21dec5aacab0784a
- worktree: /home/ubuntu/llvm-project/pulp-llvm-port/wt/19-E005
- build_dir: /home/ubuntu/llvm-project/pulp-llvm-port/build/19-E005
- branch: work/19/E005
- owner_cluster: insn-tablegen; fixup subject: `fixup! [pulp] insn-tablegen: Xpulp/Xsmallfloat/Xssr/Xdma/Xfrep instructions and sched models`
- build_targets: RISCVCommonTableGen (llvm-tblgen on RISCV.td), then LLVMRISCVCodeGen once TableGen passes
- verified leads: /home/ubuntu/llvm-project/pulp-llvm-port/steps/19/leads.md
- change note path: /home/ubuntu/llvm-project/pulp-llvm-port/notes/19/E005.md
- result path: /home/ubuntu/llvm-project/pulp-llvm-port/tasks/19/E005.result.json
- previous attempts: none

## The problem

Build error found by worker 19/E003 while running TableGen (not yet in an integration error file, because the r1 build stopped at the first duplicate): after the `uimm6_lsb0` clash, `llvm-tblgen` on the RISC-V target stops with `def already exists: powerOf2Minus1` (llvm/lib/Target/RISCV/RISCVInstrInfoXpulp.td:67), and next with `def already exists: trailing1sPlus1`. Per worker E003 both names were added upstream by 56f0ecd6db92 (CORE-V XCValu) in RISCVInstrInfoXCV.td. Same class of problem as E003 (see notes/19/E003.md on branch work/19/E003 for the approach taken there). Possibly more duplicate defs follow once these two are fixed: fix every `def already exists` duplicate between fork .td files and upstream in this task, since they share this one root cause (upstream vendor extensions now defining helper records with the fork's names); list each in the change note.

Conductor guidance: compare each fork definition with upstream's. If they differ in behaviour, rename the fork's record to a fork-private name (e.g. suffix `_pulp` / `Pulp`) and update every fork use, leaving upstream untouched. If they are exactly equivalent, still prefer the fork-private rename (behaviour-preserving, no deletion of fork code, rule 9), and state in the change note that they are equivalent so the owner can later choose to dedupe. Note: E003 (uimm6_lsb0 rename) is being reviewed in parallel and is not yet on the integration head; to reproduce past that first error, apply its rename locally only for testing (do NOT commit it on your branch) or cherry-pick nothing and use llvm-tblgen on a scratch copy as E003 did. Your commit must contain only your own change.


## Procedure

Follow /home/ubuntu/llvm-project/pulp-llvm-port/agent-sops/worker.sop.md with the parameters above.
Path mapping: documents that say /local/home/albsposi/pulp-llvm-port mean /home/ubuntu/llvm-project/pulp-llvm-port. Builds run natively (no container).
