# Task 19/E004

Written by the conductor. One task is one root cause.

- step: 19
- task_id: E004
- role: worker
- step_base_tag: llvmorg-19.1.7
- prev_tag: e6c3289804a67ea0bb6a86fadbe454dd93b8d855
- integration_head: 7c27289bc3a1a39d0727f00d21dec5aacab0784a
- worktree: /home/ubuntu/llvm-project/pulp-llvm-port/wt/19-E004
- build_dir: /home/ubuntu/llvm-project/pulp-llvm-port/build/19-E004
- branch: work/19/E004
- owner_cluster: insn-tablegen; fixup subject: `fixup! [pulp] insn-tablegen: Xpulp/Xsmallfloat/Xssr/Xdma/Xfrep instructions and sched models`
- build_targets: RISCVTargetParserTableGen (or LLVMTargetParser), RISCVCommonTableGen
- verified leads: /home/ubuntu/llvm-project/pulp-llvm-port/steps/19/leads.md
- change note path: /home/ubuntu/llvm-project/pulp-llvm-port/notes/19/E004.md
- result path: /home/ubuntu/llvm-project/pulp-llvm-port/tasks/19/E004.result.json
- previous attempts: none

## The problem

Known defect raised by the registration conflict resolver (ESCALATE in notes/19/conflict-registration.md, PROGRESS.md escalations), not yet a build error: the fork's 21 smallfloat extensions (`xfalthalf` ... `xfexpauxvecaltquarter`) were entries in the fork's LLVM 18 hand-written ISAInfo extension table (see `git show port/18:llvm/lib/Support/RISCVISAInfo.cpp` in $LLVM_SRC, with their versions). At 19 that table is generated by TableGen only from `RISCVExtension` records in the .td files (upstream `80628ee0d555`), but these 21 features are declared as plain `SubtargetFeature` in `llvm/lib/Target/RISCV/RISCVInstrInfoXsmallfloatGen.td`, so `-march=..._xfalthalf...` and the fork's RISCVISAInfo unit test expectations would be rejected at 19.

Conductor decision: convert the 21 definitions by hand to `RISCVExtension<"<name>", <major>, <minor>, "<desc>">` records (keeping each feature's record name, `Has*` field name, implied features and description exactly, and the version from the 18 table), following how the fork's other vendor extensions were converted in the registration pick (e.g. `xpulpv` in RISCVFeatures.td) and how CORE-V `FeatureVendorXCV*` is declared. The file header says it was generated by an external tool (noah95/riscv-opcodes parse_opcodes) that is not in the fork, so regeneration is not possible here; record that in the change note. Verify with `llvm-tblgen`/the RISCV TargetParser generation (target `RISCVTargetParserTableGen` or `LLVMTargetParser`) that the generated extension table now contains all 21 names with the 18 versions, and that `llc -mtriple=riscv32 -mattr=+xfalthalf` (or equivalent) is accepted once llc builds (if it does not build yet because of other clusters, say so in the result file). Owner cluster: insn-tablegen (the file lives there).


## Procedure

Follow /home/ubuntu/llvm-project/pulp-llvm-port/agent-sops/worker.sop.md with the parameters above.
Path mapping: documents that say /local/home/albsposi/pulp-llvm-port mean /home/ubuntu/llvm-project/pulp-llvm-port. Builds run natively (no container).
